## Залить на гит

```
git add .
git commit -m "Сообщение"
git push origin main
```

## Выгрузить проект

```
git clone https://github.com/MichailFlix/shop-furniture.git
cd shop-furniture
python -m venv venv
venv\Scripts\activate
pip install -r requarements.txt
```